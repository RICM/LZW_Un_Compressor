#include <stdio.h>
#include <stdlib.h>

#include "dictionary.h"

int main (int argc, char **argv){
	pTree Dictionary[259];
	pTree elem1 = malloc(sizeof(tree));
	pTree elem2 = malloc(sizeof(tree));
	pTree elem3 = malloc(sizeof(tree));
	printf("hello\n");
}